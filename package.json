{
  "name": "WasatchEscapes",
  "version": "1.0.0",
  "description": "Git and NPM",
  "main": "Wasatchindex.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run lite",
    "lite": "lite-server",
    "imagemin": "imagemin img/* -o dist/img",
    "clean": "rimraf dist",
    "build": "npm run clean && npm run imagemin && npm run copyfonts && npm run usemin",
    "usemin": "usemin mobile.html -d dist --htmlmin -o dist/mobile.html && usemin aboutus.html -d dist --htmlmin -o dist/aboutus.html && usemin Wasatchindex.html -d dist --htmlmin -o dist/Wasatchindex.html && usemin maps.html -d dist --htmlmin -o dist/maps.html",
    "copyfonts": "copyfiles -f node_modules/font-awesome/fonts/* dist/fonts"
  },
  "repository": {
    "type": "git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.2.0",
    "imagemin-cli": "^5.1.0",
    "lite-server": "^2.5.4",
    "onchange": "^7.0.0",
    "parallelshell": "3.0.1",
    "rimraf": "^3.0.2",
    "usemin-cli": "^0.6.0"
  }
}
